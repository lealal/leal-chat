import { RefObject } from 'react';
declare const useDetectScrollPosition: (ref: RefObject<HTMLElement>) => {
    detectTop: () => boolean;
    detectBottom: () => boolean;
};
export default useDetectScrollPosition;
