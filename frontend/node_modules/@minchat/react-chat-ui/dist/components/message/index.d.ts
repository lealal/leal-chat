import { UserType } from '../../types/UserType';
import { MediaType } from '../../types/MessageType';
export type Props = {
    showTimestamp?: boolean;
    created_at?: Date;
    seen?: boolean;
    text?: string;
    media?: MediaType;
    loading?: boolean;
    type?: "incoming" | "outgoing";
    user?: UserType;
    showAvatar?: boolean;
    showHeader?: boolean;
    last?: boolean;
    single?: boolean;
    clusterFirstMessage?: boolean;
    clusterLastMessage?: boolean;
    themeColor?: string;
    enableMarkdown?: boolean;
};
export default function Message({ text, media, created_at, seen, loading, type, user, showAvatar, showHeader, last, single, clusterFirstMessage, clusterLastMessage, showTimestamp, themeColor, enableMarkdown }: Props): import("react/jsx-runtime").JSX.Element;
