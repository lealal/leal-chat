import { MediaType } from '../../../types/MessageType';
interface Props extends MediaType {
    last?: boolean;
    single?: boolean;
    messageType: "incoming" | "outgoing";
}
export default function MediaContent({ type, url, size, last, single, messageType }: Props): import("react/jsx-runtime").JSX.Element;
export {};
