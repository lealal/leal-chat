import { Props } from '..';
export default function IncomingMessage({ text, media, user, showAvatar, showHeader, last, single, created_at, showTimestamp, themeColor, enableMarkdown }: Omit<Props, "type" | "clusterFirstMessage" | "clusterLastMessage" | "seen">): import("react/jsx-runtime").JSX.Element;
