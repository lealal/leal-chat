import { default as React } from 'react';
export type Props = {
    children: React.ReactNode;
};
export default function MessageContainer({ children }: Props): import("react/jsx-runtime").JSX.Element;
